import static org.junit.jupiter.api.Assertions.*;
import org.json.*;

import org.junit.Before;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/*class TicketViewerTests {
	
	/*@Before
	void setUp() {
		TicketViewer.getTicketsFromAPI();
		TicketViewer.setGlobals();
	}  

	@Disabled
	@Test
	void badAPICredentials() {
		Throwable e = assertThrows(Exception.class, () -> TicketViewer.getTicketsFromAPI());
		assertEquals("Error connecting to API. Exiting program.", e.getMessage());
	}
	
	@Test
	// Test setGlobals
	void ticketNumUnder100() {
		TicketViewer.getTicketsFromAPI();
		TicketViewer.setGlobals();
		assertTrue(TicketViewer.getNumPages() == 4);
		assertTrue(TicketViewer.getNumTickets() == 95);
		
	}
	
	@Test
	void pagePartitionTest() {
		TicketViewer.getTicketsFromAPI();
		TicketViewer.setGlobals();
		TicketViewer.pagePartition();
		assertTrue(TicketViewer.getTicketPages().size() == 4);

	}

}*/
